version: '3.8'

services:
  ytflow:
    build: .
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - NODE_ENV=production
    restart: unless-stopped
    volumes:
      # Optional: persist nothing (stateless)
      - /tmp:/tmp
